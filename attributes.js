const parentElement = document.querySelector("#parent");

parentElement.setAttribute("class", "text-center");
console.log(parentElement.getAttribute("class"));

parentElement.removeAttribute("class");
